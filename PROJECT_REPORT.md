# UI-TARS LoRA 项目工作报告

**项目：** UI-TARS Per-Application Grounding LoRA<br>
**报告日期：** 2026-07-29<br>
**范围：** 截至当前 `master` 分支（31 个提交）的代码、配置模板、运行产物与聚合评估记录。

## 1. 项目目标与成果概览

本项目基于本地 `ByteDance-Seed/UI-TARS-1.5-7B`，构建了面向单个应用的 UI
元素定位微调流程。模型接收截图和元素描述，输出一个原始截图像素空间中的坐标点：

```text
screenshot + UI element description -> (x, y)
```

项目的核心交付不是单次训练，而是一套可按应用、数据版本和训练运行独立复现的
QLoRA 工作流。它已覆盖数据准备、训练、断点恢复、适配器发布、生成式评估、结果
归档和本地部署，并始终与生产 UI-TARS 服务隔离。

截至报告日，Avantage 应用已完成 v1、v2、v4 三个数据快照的基线和/或 LoRA
对比实验。v4 的视觉投影层 LoRA 实验取得当前最佳聚合结果：在 65 条验证标签上，
框内命中率从原生模型的 53.85% 提升到 66.15%，平均预测点到标注框中心的距离从
141.55 像素降低到 81.97 像素。

## 2. 已完成的工程工作

### 2.1 数据契约与数据治理

- 建立严格的 bbox 标注 CSV 契约：记录 ID、相对图像路径、元素描述、`left/top/right/bottom`
  和应用版本；其中左/上边界包含、右/下边界排除。
- 将训练目标统一为 bbox 几何中心，按整数四舍五入后以原始截图像素坐标输出；不使用
  处理器缩放后的坐标系或固定画布坐标。
- 实现标注字段、图像存在性、图像尺寸、bbox 合法性、记录 ID 唯一性和目录边界校验；
  空白应用版本统一归一为 `unknown`。
- 按标签行进行确定性 80/20 训练/验证切分，并将输入哈希、切分 ID、图像清单、坐标
  契约和跨切分图像复用情况写入 `manifest.json`。
- 实现每应用独立数据根目录和 `vN` 不可变数据快照，训练与评估会校验所选应用和数据
  版本与清单一致，避免跨应用或跨快照混用。

### 2.2 训练与模型适配

- 以 UI-TARS-1.5-7B 本地副本为基础，使用 NF4 4-bit QLoRA、bfloat16 和 PEFT 完成
  单点定位监督微调；视觉编码器始终冻结。
- 实现多样本 batch 的多模态数据整理器，确保图像、提示词和标签对齐，并只对 assistant
  坐标响应计算损失。
- 默认在语言模型注意力和 MLP 投影模块上训练 LoRA；新增可选的视觉 merger MLP
  两层 LoRA，同时明确排除视觉 patch embedding 和视觉 block。
- 使用 xFormers 替换冻结视觉编码器的稠密注意力实现，降低视觉训练时的注意力内存压力。
- 每个应用和运行名称拥有独立的输出目录；检查点、训练日志、TensorBoard、冻结配置、
  数据清单、最佳适配器和最新适配器分开保存。新运行拒绝覆盖已有产物，恢复运行会验证
  冻结配置和数据清单后选择最新检查点。

### 2.3 评估、产物与部署

- 建立统一评分逻辑：解析模型响应中的前两个十进制整数，检查坐标是否在原图范围内，
  并以预测点是否落在 bbox 内计算总分母准确率。
- 自动生成可解析率、坐标范围合规率、bbox 命中率、中心距离、相对图像对角线误差及其
  均值/中位数/P90；可按应用版本汇总。
- 每次 epoch 评估后生成轻量化生成式评估记录，不保存提示词、模型回复、截图或 bbox
  标签，仅保存记录 ID 与聚合诊断值。
- 将独立评估统一为本地 Transformers/PEFT 推理路径，保证原生模型、语言 LoRA 和含
  视觉模块 LoRA 的适配器使用一致的加载和推理方式；同时保留基于已有响应文件的离线评分。
- 支持将语言 LoRA 适配器作为本地 vLLM 服务启动，并强制校验适配器所属应用；默认端口
  为 18001，禁止使用生产服务保留的 18000。

### 2.4 运行安全与可复现性

- 固定职责分配：GPU 0 默认训练，GPU 1 默认临时评估和本地适配器服务，生产 vLLM
  服务保留在 GPU 2 和端口 18000；所有入口均支持显式 GPU 选择。
- 训练前运行环境检查，确认生产服务的预期进程仍在运行、基座模型来源可用并报告存储与
  GPU 状态。模型复制脚本拒绝覆盖已有模型副本，并将副本设为只读。
- 隔离训练环境从生产服务环境的文件系统副本创建，固定关键 Python 依赖版本；同时解决
  TensorBoard 与新版 `setuptools` 的兼容性问题。
- 提供上游 Qwen2.5-VL 源码提交锁定脚本，避免可复现实验依赖浮动分支；项目也保留
  Agent-S、UI-TARS 和 Qwen3-VL 参考实现以支持问题追溯。

## 3. 实现演进

项目从 2026-07-14 至 2026-07-27 逐步完成 31 个提交，可归纳为以下阶段：

| 阶段 | 主要完成内容 |
| --- | --- |
| 初始工作流 | 建立本地模型复制、运行时保护、环境创建、配置加载、数据准备和 QLoRA 训练骨架。 |
| 数据与应用隔离 | 引入应用级 LoRA 输出、YAML 作为训练参数单一事实来源、标签元数据规范化及简化的数据目录。 |
| 定位契约与评估 | 固化原始像素坐标单点契约，增加自动生成式评估、bbox 命中和距离诊断。 |
| 可恢复运行 | 将部署适配器、Trainer 检查点和运行记录分离，支持配置/数据清单冻结、断点恢复和逐 epoch 指标。 |
| 可运维性 | 加入 GPU 参数、数据集版本、相对适配器路径和应用身份校验，保护生产服务并保持实验隔离。 |
| 视觉 LoRA 与评估统一 | 将视觉 merger MLP LoRA 设计为可选能力，修正视觉 LoRA 评估，并最终统一为 Transformers 推理。 |

## 4. 数据与实验设计

所有数据快照均使用相同的确定性切分种子和 80/20 行级切分。下表仅包含样本数量等
聚合元数据，不包含截图、标注文本、图像路径或记录 ID。

| 数据快照 | 总标签数 | 训练标签数 | 验证标签数 | 跨切分复用截图数 | 已有结果 |
| --- | ---: | ---: | ---: | ---: | --- |
| Avantage v1 | 253 | 202 | 51 | 3 | 原生基线、语言 LoRA |
| Avantage v2 | 178 | 142 | 36 | 3 | 原生基线、语言 LoRA |
| Avantage v4 | 324 | 259 | 65 | 3 | 原生基线、语言 LoRA、视觉投影层 LoRA |
| Avantage v5 | 324 | 259 | 65 | 3 | 已生成处理数据，尚无归档评估运行 |

已归档训练运行采用 5 个 epoch、学习率 `1e-4`、LoRA rank 16、LoRA alpha 32 和
dropout 0.05。v2 和 v4 使用每设备 batch size 1、梯度累积 6；早期 v1 运行使用梯度
累积 8。v4_2 在默认语言模块 LoRA 之外开启视觉 merger MLP LoRA。

评估使用当前运行的 `adapters/best` 适配器；不存在适配器目录时才显式回退到原生
UI-TARS 模型。生成采用贪婪解码，保证实验比较不受随机采样影响。

## 5. 实验结果

下表中“框内命中率”按全部验证标签计算；“平均中心距离”仅对解析成功且坐标在原图
范围内的响应计算。各行验证输出均为 100% 可解析。除最早的 v1 原生基线报告未单独
记录坐标范围合规率外，其余报告的坐标范围合规率均为 100%。

| 数据快照 | 模型/适配方式 | 验证标签数 | 框内命中 | 框内命中率 | 平均中心距离（像素） | 相对对角线误差均值 |
| --- | --- | ---: | ---: | ---: | ---: |
| v1 | 原生 UI-TARS | 51 | 30 | 58.82% | 79.96 | 3.70% |
| v1 | 语言 LoRA | 51 | 37 | 72.55% | 22.00 | 1.02% |
| v2 | 原生 UI-TARS | 36 | 25 | 69.44% | 72.43 | 3.35% |
| v2 | 语言 LoRA | 36 | 26 | 72.22% | 24.90 | 1.15% |
| v4 | 原生 UI-TARS | 65 | 35 | 53.85% | 141.55 | 6.55% |
| v4 | 语言 LoRA | 65 | 38 | 58.46% | 104.54 | 4.84% |
| v4 | 语言 LoRA + 视觉投影层 LoRA | 65 | 43 | 66.15% | 81.97 | 3.79% |

结果显示：

- 在 v1 上，语言 LoRA 相比原生基线增加 7 个 bbox 命中，提升 13.73 个百分点。
- 在 v2 上，语言 LoRA 将平均中心距离由 72.43 像素降至 24.90 像素；bbox 命中率
  提升 2.78 个百分点。
- 在 v4 上，语言 LoRA 已较原生基线提升 4.62 个百分点；启用视觉投影层 LoRA 后，
  又提升 7.69 个百分点，总提升 12.30 个百分点，并同时改善位置误差。
- v4_2 的最佳验证损失为 0.1577，低于 v4_1 的 0.1807。该变化与更高的生成式框内
  命中率方向一致，但模型选择仍以冻结运行产物中记录的验证损失为准。

这些数字用于各自数据快照内的受控比较。不同快照的标注规模和组成不同，因此不将
v1、v2、v4 的绝对指标解释为横向排名。

## 6. 当前局限与后续建议

当前验证切分以标签行为单位。同一截图可承载多个 UI 元素标签，因此已评估快照中均有
3 张截图同时出现在训练与验证集合。该设计满足单标签定位样本的确定性划分需求，但会
使验证图像并非完全独立，并可能高估对未见截图的泛化能力。

建议下一阶段按以下优先级推进：

1. 增加按截图分组的独立验证/测试切分，并与现有行级切分并列报告，量化图像复用的影响。
2. 扩展到更多应用和更多应用版本，在统一的独立测试集上检验“一应用一适配器”的迁移性。
3. 对视觉投影层 LoRA 做多随机种子重复和消融试验，分离数据快照差异、训练波动与模块
   选择对结果的贡献。
4. 将 v5 作为下一次不可变实验快照，使用新的运行名称完成训练、独立评估和结果归档，
   不覆盖任何历史运行。

## 7. 证据与复现入口

报告中的流程定义来自 `README.md`、`configs/grounding_contract.json`、应用配置模板和
`scripts/` 下的训练、评估与运行产物脚本。实验样本量来自对应快照的
`processed/manifest.json`，指标来自各训练运行的聚合评估 JSON 与 epoch 评估记录。

可按以下顺序复现新的应用实验：

```bash
scripts/check_runtime.sh
scripts/copy_model.sh
scripts/pin_sources.sh
python3 scripts/prepare_grounding_data.py --config configs/apps/<app_id>.yaml
scripts/create_environment.sh
scripts/run_train.sh --gpu 0 configs/apps/<app_id>.yaml
/root/autodl-tmp/xukefan/miniconda3/envs/ui-tars-lora/bin/python \
  scripts/evaluate_grounding.py --config configs/apps/<app_id>.yaml --gpu 1
```

执行时应为新实验设置新的 `run_name`，并保持生产服务所在的 GPU 2 与端口 18000 不被
训练或临时评估占用。
